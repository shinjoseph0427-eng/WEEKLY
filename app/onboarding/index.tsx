// Onboarding (18+ gate, photo, location) — web OnboardingFlow.jsx.
// Body is a placeholder this phase; the routing gate sends new users here.
import { Placeholder } from '../../src/ui/Placeholder';

export default function OnboardingScreen() {
  return <Placeholder title="Welcome to WEEKLY" note="Let's set up your profile." />;
}
