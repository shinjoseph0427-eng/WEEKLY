// Blocked users management (v1 requirement). Placeholder body this phase;
// block/unblock logic lives in src/features/safety/safety.ts.
import { Placeholder } from '../../src/ui/Placeholder';

export default function BlockedScreen() {
  return <Placeholder title="Blocked Users" note="Manage who you've blocked." />;
}
